---
// src/pages/index.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import Sticker from '../components/Sticker.astro';
---

<BaseLayout title="Studio.lib | System Router">
    <div class="viewport-wrapper">
        
        <div class="sticker-layer">
            <Sticker type="QC_PASSED" top="15vh" left="10vw" rotate="-15" />
            <Sticker type="LOGIC_GATE" top="70vh" left="12vw" rotate="20" />
            <Sticker type="HIGH_VOLTAGE" top="40vh" left="82vw" rotate="10" />
        </div>

        <main class="schematic-container">
            <div class="schematic-header">
                <div class="coord-marker">X:00 Y:00</div>
                <h1>Studio.lib</h1>
                <p class="subtitle">PROJECT: Digital Logic & Analog Log</p>
            </div>

            <nav class="schematic-grid">
                <a href="/cv" class="schematic-node">
                    <span class="pin">PIN 00</span>
                    <div class="node-data"><strong>CV_PORTFOLIO</strong><br/>Hardware architectures</div>
                </a>
                <a href="/silicon" class="schematic-node">
                    <span class="pin">PIN 01</span>
                    <div class="node-data"><strong>SILICON_ARCHIVE</strong><br/>NoC & Interfaces</div>
                </a>
                <a href="/archive" class="schematic-node">
                    <span class="pin">PIN 10</span>
                    <div class="node-data"><strong>ANALOG_LOG</strong><br/>Loose-leaf binder index</div>
                </a>
                <a href="https://github.com/당신의깃허브아이디" target="_blank" rel="noopener noreferrer" class="schematic-node">
                    <span class="pin">PIN 11</span>
                    <div class="node-data"><strong>GITHUB_REPO</strong><br/>Source codes & controls</div>
                </a>
            </nav>
            <footer class="io-interfaces">
                <div class="io-title">EXTERNAL I/O PORTS //</div>
                <div class="io-ports-grid">
                    <a href="mailto:your.email@example.com" class="io-port">
                        <span class="port-type">[MAIL]</span>
                        <span class="port-val">TX/RX_Request</span>
                    </a>
                    <a href="https://github.com/당신의아이디" target="_blank" rel="noopener noreferrer" class="io-port">
                        <span class="port-type">[GIT]</span>
                        <span class="port-val">Source_Control</span>
                    </a>
                    <a href="https://instagram.com/당신의아이디" target="_blank" rel="noopener noreferrer" class="io-port">
                        <span class="port-type">[SNS]</span>
                        <span class="port-val">Analog_Life_Log</span>
                    </a>
                </div>
            </footer>
        </main>
    </div>
</BaseLayout>

<style>
    /* 1. 전역 모눈종이 배경 설정 */
    :global(body) {
        margin: 0;
        background-color: #F4EBD0;
        background-image: 
            linear-gradient(#E0D5B7 1px, transparent 1px),
            linear-gradient(90deg, #E0D5B7 1px, transparent 1px);
        background-size: 20px 20px;
        font-family: 'Noto Serif KR', serif;
    }

    /* 2. 전역 래퍼: 중앙 정렬 및 스크롤바 오버플로우 방지 */
    .viewport-wrapper {
        position: relative;
        width: 100%;
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden; /* 스티커가 화면 밖으로 나갈 때 생기는 가로 스크롤 차단 */
        padding: 2rem;
        box-sizing: border-box;
    }

    /* 3. 스티커 전용 레이어 */
    .sticker-layer {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none; /* 스티커가 클릭 이벤트를 탈취하는 현상 방지 */
        z-index: 5;
    }

    /* 4. 중앙 메인 컨테이너 (라우터 보드) */
    .schematic-container {
        position: relative; 
        width: 100%;
        max-width: 600px;
        background: rgba(244, 235, 208, 0.95); 
        border: 2px solid #4A3F35;
        padding: 2rem;
        box-shadow: 10px 10px 0px rgba(74, 63, 53, 0.1);
        z-index: 10; /* 스티커 레이어(5)보다 높게 설정하여 메인보드 정보 가림 방지 */
    }
    
    .coord-marker { 
        position: absolute; 
        top: -10px; 
        left: -10px; 
        background: #F4EBD0; 
        padding: 0 5px; 
        font-family: 'JetBrains Mono', monospace; 
        font-size: 0.8rem; 
        color: #8C6F56; 
        border: 1px solid #4A3F35;
        z-index: 20;
    }
    .schematic-header { 
        border-bottom: 2px solid #4A3F35; 
        padding-bottom: 1rem; 
        margin-bottom: 2rem; 
    }
    .schematic-header h1 { 
        font-size: 2.5rem; 
        margin: 0; 
        color: #4A3F35; 
        letter-spacing: -1px;
    }
    .subtitle { 
        font-family: 'JetBrains Mono', monospace; 
        font-size: 0.8rem; 
        color: #8C6F56; 
        margin-top: 5px; 
    }

    .schematic-grid { 
        display: flex; 
        flex-direction: column; 
        gap: 1.2rem; 
    }
    .schematic-node {
        display: flex;
        align-items: center; /* 다시 중앙 정렬로 롤백 */
        text-decoration: none;
        color: #4A3F35;
        border: 1px solid #4A3F35;
        transition: background 0.15s ease-in-out;
        background: rgba(255, 255, 255, 0.5);
        position: relative;
    }
    .schematic-node:hover { 
        background: #4A3F35; 
        color: #F4EBD0; 
    }
    .pin { 
        padding: 1rem; 
        background: #4A3F35; /* 타이틀 색상과 동일하게 설정 */
        color: #F4EBD0; /* 가독성을 위해 밝은 대비색 사용 */
        font-family: 'JetBrains Mono', monospace; 
        font-size: 0.8rem; 
        border-right: 1px solid #4A3F35; 
        min-width: 60px;
        text-align: center;
    }
    .node-data { 
        padding: 0.8rem 1rem; 
        font-size: 0.9rem; 
        font-family: 'JetBrains Mono', monospace; 
        line-height: 1.4;
    }
    .schematic-node:hover .pin { 
        background: #F4EBD0; /* 호버 시 전체 배경색으로 반전 */
        color: #4A3F35; 
    }
    .io-interfaces {
        margin-top: 2rem;
        padding-top: 1rem;
        border-top: 1px dashed #4A3F35; /* 데이터시트의 절취선 느낌 */
    }
    .io-title {
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem;
        color: #8C6F56;
        margin-bottom: 0.8rem;
        letter-spacing: 1px;
    }
    .io-ports-grid {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }
    .io-port {
        display: flex;
        align-items: center;
        text-decoration: none;
        color: #4A3F35;
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.85rem;
        transition: transform 0.1s ease;
    }
    .io-port:hover {
        color: #D32F2F; /* 동작(Active) 상태의 붉은색 인디케이터 */
        transform: translateX(3px); /* 마우스 오버 시 미세한 물리적 피드백 */
    }
    .port-type {
        font-weight: bold;
        margin-right: 0.4rem;
    }
    .port-val {
        opacity: 0.8;
        text-decoration: underline;
        text-underline-offset: 3px;
    }

    /* 5. 모바일/태블릿 대응 (반응형 예외 처리) */
    @media (max-width: 1000px) {
        /* 뷰포트 너비가 1000px 이하로 좁아질 경우 스티커 숨김 */
        .sticker-layer {
            display: none;
        }
    }

    @media (max-width: 600px) {
        .viewport-wrapper {
            padding: 1rem;
            /* 세로 스크롤 허용, 가로 스크롤 방지 */
            overflow-x: hidden;
            overflow-y: auto;
            align-items: stretch; /* 상하단 꽉 차게 프레임을 화면에 맞춤 */
        }
        
        .schematic-container {
            padding: 1.5rem;
            box-shadow: 4px 4px 0px rgba(74, 63, 53, 0.1); /* 꽉 차는 프레임에 맞게 그림자 조정 */
            display: flex;
            flex-direction: column;
            justify-content: flex-start; /* 내부 콘텐츠는 위에서부터 정렬 */
        }

        .schematic-header h1 {
            font-size: 2rem; /* 폰트 크기 축소 */
        }

        .pin {
            padding: 0.8rem;
            min-width: 50px;
        }

        .node-data {
            padding: 0.6rem 0.8rem;
            font-size: 0.85rem;
        }
    }
</style>